Subject: [PATCH] hw/arm/virt: register the NVIDIA BPMP guest bridge

Only the registration hooks. The device itself lives as ordinary source in
../sources/{hw/misc/nvidia_bpmp_guest.c,include/hw/misc/nvidia_bpmp_guest.h}
and is copied in by package.nix's postPatch.

Rebased from https://github.com/tiiuae/ghaf PR #1240 (QEMU 9.2) onto 11.0.1.
The base address moved from 0x090c0000 to 0x090d0000: in QEMU >= 10 that slot
is VIRT_ACPI_PCIHP, which ARM_VIRT selects, so the original address overlapped
a live MMIO region. Guests must set the bpmp node's `virtual-pa` to 0x090d0000.

--- a/hw/arm/Kconfig
+++ b/hw/arm/Kconfig
@@ -36,6 +36,7 @@
     select VIRTIO_MEM_SUPPORTED
     select ACPI_CXL
     select ACPI_HMAT
+    select NVIDIA_BPMP_GUEST
 
 config CUBIEBOARD
     bool
--- a/hw/arm/virt.c
+++ b/hw/arm/virt.c
@@ -91,6 +91,7 @@
 #include "hw/virtio/virtio-md-pci.h"
 #include "hw/virtio/virtio-iommu.h"
 #include "hw/char/pl011.h"
+#include "hw/misc/nvidia_bpmp_guest.h"
 #include "hw/cxl/cxl.h"
 #include "hw/cxl/cxl_host.h"
 #include "qemu/guest-random.h"
@@ -196,6 +197,7 @@
     [VIRT_PVTIME] =             { 0x090a0000, 0x00010000 },
     [VIRT_SECURE_GPIO] =        { 0x090b0000, 0x00001000 },
     [VIRT_ACPI_PCIHP] =         { 0x090c0000, ACPI_PCIHP_SIZE },
+    [VIRT_NVIDIA_BPMP_GUEST] =  { 0x090d0000, 0x00001000 },
     [VIRT_MMIO] =               { 0x0a000000, 0x00000200 },
     /* ...repeating for a total of NUM_VIRTIO_TRANSPORTS, each of that size */
     [VIRT_PLATFORM_BUS] =       { 0x0c000000, 0x02000000 },
@@ -1180,6 +1182,13 @@
     hwaddr size = vms->memmap[VIRT_MMIO].size;
     MachineState *ms = MACHINE(vms);
 
+    /*
+     * NVIDIA BPMP guest bridge. Forwards the guest's BPMP messages to the
+     * host proxy through /dev/bpmp-host. Harmless where that device does not
+     * exist: instance_init logs and leaves host_device_fd < 0.
+     */
+    nvidia_bpmp_guest_create(vms->memmap[VIRT_NVIDIA_BPMP_GUEST].base);
+
     /* We create the transports in forwards order. Since qbus_realize()
      * prepends (not appends) new child buses, the incrementing loop below will
      * create a list of virtio-mmio buses with decreasing base addresses.
--- a/hw/misc/Kconfig
+++ b/hw/misc/Kconfig
@@ -11,6 +11,9 @@
 config ARMSSE_CPU_PWRCTRL
     bool
 
+config NVIDIA_BPMP_GUEST
+    bool
+
 config ISA_DEBUG
     bool
     depends on ISA_BUS
--- a/hw/misc/meson.build
+++ b/hw/misc/meson.build
@@ -1,4 +1,5 @@
 system_ss.add(when: 'CONFIG_APPLESMC', if_true: files('applesmc.c'))
+system_ss.add(when: 'CONFIG_NVIDIA_BPMP_GUEST', if_true: files('nvidia_bpmp_guest.c'))
 system_ss.add(when: 'CONFIG_EDU', if_true: files('edu.c'))
 system_ss.add(when: 'CONFIG_FW_CFG_DMA', if_true: files('vmcoreinfo.c'))
 system_ss.add(when: 'CONFIG_ISA_DEBUG', if_true: files('debugexit.c'))
--- a/include/hw/arm/virt.h
+++ b/include/hw/arm/virt.h
@@ -72,6 +72,7 @@
     VIRT_PCIE_MMIO,
     VIRT_PCIE_PIO,
     VIRT_PCIE_ECAM,
+    VIRT_NVIDIA_BPMP_GUEST,
     VIRT_PLATFORM_BUS,
     VIRT_GPIO,
     VIRT_UART1,
